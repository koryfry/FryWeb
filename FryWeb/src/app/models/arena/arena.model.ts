/**
 * Arena represents an instance of an Ice Hockey Arena where games are played at
 */

export interface Arena {
    id?: number,
    name: string,  
    address: string,
    city: string,
    state: string,
    zipCode: string
}
