/**
 * Icon represents an instance of an icon that helps the user navigate to different areas of the app
 */

 export interface Icon {
   name: string,
   tooltipText: string,
   tooltipPosition: string,
   backgroundColor: string,
   textColor: string,
   avatarName: string
 }